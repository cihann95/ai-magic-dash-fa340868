# T0.3 — CI Pipeline Durum Tespiti

## Commit Geçmişi
- **Orijinal commit**: `79b2e1a` — `ci(crash-test): add GitHub Actions workflow for automated crash-test gatekeeper`
- **Revert commit**: `a505afd` — `revert: ci — revert CI workflow due to GitHub OAuth scope limitation`
- **Dosya**: `.github/workflows/ci.yml` (33 satır, 1 dosya)

## CI Workflow Yapısı
```yaml
name: crash-test
on:
  push:
    branches: [main]
    paths-ignore: ["*.md", "*.txt", "LICENSE", ".gitignore", ".env.example"]
  pull_request:
    branches: [main]
jobs:
  crash-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
      - name: Run audit suite
        run: deno run --allow-net --allow-env --allow-read scripts/audit/_run_all.ts
```

## Başarısızlık Nedenleri
1. **OAuth Token Scope Eksikliği**: GitHub OAuth token (`gho_****`) sadece `gist, read:org, repo` scope'larına sahip. `workflow` scope'u yok → workflow dosyası push edilemedi.
2. **GitHub API Hatası**: 403 Forbidden — `gh: Warning: Insufficient OAuth scopes to list workflows`
3. **Başka hata**: Yok — CI workflow'u daha önce hiç çalışmamıştı, sadece push edilememişti.

## Root Cause
GitHub CLI OAuth token'ı `workflow` scope'u olmadan oluşturulmuş. `gh auth refresh -h github.com -s workflow` ile düzeltilmeli.

## Düzeltme Önerisi
```bash
gh auth refresh -h github.com -s workflow
```
Sonra commit'i tekrar push et.

## Durum: PASS
- CI workflow yapısı doğru
- Audit script'leri mevcut (`scripts/audit/_run_all.ts`)
- Sadece OAuth scope eksik — teknik sorun değil, konfigürasyon sorunu
