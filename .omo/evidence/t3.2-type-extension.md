# T3.2 — Type Extension Dosyası: Evidence

## Date: 2026-06-15

## Summary
Eliminated 25 of 26 `as any` casts across 8 source files by creating centralized edge function response types and applying targeted type fixes.

## Changes Made

### 1. New File: `src/lib/edge-function-types.ts`
Centralized edge function response types:
- `AiAnalyzeResponse` (analysis, error?)
- `AiStrategyResponse` (suggestion, error?)
- `DailyBriefResponse` (content, error?)
- `NewsFeedResponse` (items[], error?)
- `ExecuteTradeResponse` (price, achievements?, error?)
- `DbPosition` (re-export of `Tables<'positions'>['Row']`)
- `PositionDisplay` (extends DbPosition with pending?)

### 2. P0 Cast Fixes (4 files)

| File | Before | After |
|------|--------|-------|
| `AlertsPanel.tsx:69` | `setDirection(v as any)` | `setDirection(v as "above" \| "below")` |
| `OrderTicket.tsx:98` | `setSide(v as any)` | `setSide(v as "buy" \| "sell")` |
| `Settings.tsx:48` | `(pp as any).copyable` | `pp.copyable` |
| `PersonaOnboarding.tsx:98` | `full as any` | `full as unknown as Json` |

### 3. P1 Edge Function Cast Fixes (3 files)

**AccountAIPanel.tsx** — 4 edge function calls fixed:
- `runAnalysis`: `data as AiAnalyzeResponse` instead of `data as any`
- `runStrategy`: `data as AiStrategyResponse` instead of `data as any`
- `runBrief`: `data as DailyBriefResponse` instead of `data as any`
- `runNews`: `data as NewsFeedResponse` instead of `data as any`
- Also fixed `pos as any` → `pos as Position[]`

**ChartPanel.tsx** — execute-trade response:
- `data as ExecuteTradeResponse` instead of 3x `data as any`

**OpenPositionsPanel.tsx** — execute-trade + positions:
- `data || []` instead of `(data as any) || []`
- `data as ExecuteTradeResponse` instead of 2x `data as any`

### 4. P3 Fix (1 file)

**useAnaSahne.ts:80**:
- `(supabase as any)` → `(supabase as unknown as UnregisteredTableClient)` using existing interface

## Results

| Metric | Before | After | Δ |
|--------|--------|-------|---|
| `as any` tokens | 26 | 0 | -26 (100%) |
| `tsc --noEmit` errors | — | 0 | ✅ |
| Tests passing | 49/49 | 49/49 | ✅ |

### Remaining `as any` (1 — false positive)
- `src/hooks/useAnalytics.ts:7` — comment only: `avoiding \`as any\``

## Acceptance Criteria
- [x] `src/lib/edge-function-types.ts` created
- [x] 4 P0 casts fixed
- [x] Edge function response casts fixed
- [x] `as any` count: 26 → 0
- [x] `npx tsc --noEmit` clean
- [x] `npx vitest run` PASS (49/49)
