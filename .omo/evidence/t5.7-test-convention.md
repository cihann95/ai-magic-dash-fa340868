# T5.7 — Test Files Consolidated Under `__tests__/` Directories

## Summary

Moved 2 test files into their respective `__tests__/` directories to enforce the
convention that all test files reside under `__tests__/`.

## Files Moved

| Original Path | New Path |
|---|---|
| `src/test/example.test.ts` | `src/test/__tests__/example.test.ts` |
| `src/test-utils/test-utils.test.ts` | `src/test-utils/__tests__/test-utils.test.ts` |

## Files NOT Moved (intentionally kept in place)

- `src/test/setup.ts` — vitest config `setupFiles: ["./src/test/setup.ts"]`
- `src/test-utils/setup.ts` — test utility setup, not a test file
- `src/test-utils/factories.ts`, `health-check.ts`, `index.ts`, `mocks.ts` — utilities, not tests

## Vitest Config

`vitest.config.ts` uses `include: ["src/**/*.{test,spec}.{ts,tsx}"]` — this glob
already matches files inside `__tests__/` directories. No config changes needed.

## Test Results

```
Test Files  10 passed (10)
     Tests  55 passed (55)
```

All 55 tests pass across all 10 test files.

## Commit

```
45948eb refactor(test): consolidate test files under __tests__/ directories
```

## Verification

- `git mv` used to preserve file history (renamed, not new)
- `npx vitest run` — 55/55 tests PASS
- `git push` to `origin/main`
