== QA Scenario 9: Waiting Room Cleanup Migration (T14) ==

File: supabase/migrations/20260610000000_cleanup_stale_rooms.sql (39 lines)

1. FUNCTION DEFINITION
   - CREATE OR REPLACE FUNCTION public.cleanup_stale_rooms()
   - RETURNS void
   - LANGUAGE plpgsql
   - SECURITY DEFINER
   - SET search_path = public
   - PASS

2. LOGIC
   - UPDATE blitz_rooms SET status = 'cancelled', updated_at = now()
   - WHERE status = 'waiting' AND created_at < now() - interval '30 minutes'
   - PASS

3. CRON JOB
   - Idempotent: unschedule first (with EXCEPTION WHEN OTHERS THEN NULL)
   - Schedule: '*/5 * * * *' (every 5 minutes)
   - Calls: SELECT public.cleanup_stale_rooms()
   - PASS

4. SQL SYNTAX
   - $$ delimiters balanced (5 occurrences)
   - BEGIN/END blocks balanced (4 occurrences)
   - DO $$ block for idempotent unschedule
   - PASS

5. SAFETY
   - SECURITY DEFINER (bypasses RLS)
   - Only affects rooms waiting > 30 minutes
   - Sets updated_at for audit trail
   - PASS

VERDICT: Migration SQL is syntactically correct and safe
